package com.mendix.recipes.domain;

public enum MeasurementUnit {
    // volume
    LITER, CUP, TABLESPOON, TEASPOON,
    // weight
    GRAM,
    // count / unmeasured
    PIECE,
    // packaged
    CAN, PACKAGE, JAR;
}
